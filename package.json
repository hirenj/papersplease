{
  "name": "papersplease",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "package": "pack-lambda --exclude-version --exclude-scope",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "googleapis": "^31.0.2",
    "lambda-helpers": "hirenj/lambda-helpers"
  },
  "bundledDependencies": [
    "googleapis",
    "lambda-helpers"
  ],
  "devDependencies": {
    "pack-lambda": "^1.0.0"
  }
}
